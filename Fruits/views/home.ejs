<%- include('./partials/header')%>

    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <% destinations.forEach(function (d) { %>
      <%= d.name %>
      <% }); %>

<%- include('./partials/footer')%>
