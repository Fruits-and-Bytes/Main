<%- include('./partials/header')%>

<section class="home">
</section>
        <% destinations.forEach(function (d) { %>
          <a href="/<%= d._id %>/show">
            <div class="projcard-container">
            <div class="projcard projcard-blue">
              <div class="projcard-innerbox">
                <img class="projcard-img" src="<%= d.destinationImage %>" />
                <div class="projcard-textbox">
                  <div class="projcard-title"><%= d.name %></div>
                  <div class="projcard-subtitle"><%= `Learn more about ${d.name}`%></div>
                  <div class="projcard-bar"></div>
                  <div class="projcard-description"><%= d.cardDescription %></div>
                  <div class="projcard-tagbox">
                    <span class="projcard-tag"><%= d.fruit %></span>
                  </div>
                </div>
              </div>
            </div> 
          </a>
          <% }); %>      
    <% if(user) { %>
      <a href="/new" class="add-section">Add A Country to Make this List More Comprehensive!</a>
    <% } else { %>
      <a href="/auth/google" class="login add-section">Login to Add Countries to this List!</a>
      <% } %>
      <br></br>
      
<style>
  .add-section {
    font-size: 2.5rem;
  }
</style>

    <script>
      document.querySelectorAll(".projcard-description").forEach(function(box) {
        $clamp(box, {clamp: 6});
      });
    </script>

<%- include('./partials/footer')%>